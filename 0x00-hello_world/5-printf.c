/**
* main -where the function or the program starts the execution
* description-the program prints a string 
* the program returns zero as the value
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
